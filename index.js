const accdianHome = document.querySelector('.accodian-home');
const hiddenMenu = document.querySelector('.hidden-menu');

console.log(hiddenMenu.style);
accdianHome.addEventListener('click', (e) => {
  // if (hiddenMenu.style.display == 'none') {
  //   for (let i = 0; i < hiddenMenu.length; i++) {
  //     console.log(i);
  //   }
  // }
});
